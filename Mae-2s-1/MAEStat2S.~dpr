program MAEStat2S;

uses
  Forms,
  MainU in 'MainU.pas' {fmMain},
  ExperementDataU in 'ExperementDataU.pas',
  ExportFunctionsU in 'ExportFunctionsU.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TfmMain, fmMain);
  Application.Run;
end.
